import SwiftUI

struct ListView: View {
    var body: some View {
        VStack {
            Text("List")
                .font(.largeTitle)
                .padding()
        }
        .background(Color.white)
    }
}

#Preview {
    ListView()
}
