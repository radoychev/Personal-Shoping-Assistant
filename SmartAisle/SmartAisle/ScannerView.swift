import SwiftUI

struct ScannerView: View {
    var body: some View {
        VStack {
            Text("Scanner")
                .font(.largeTitle)
                .padding()
        }
        .background(Color.white)
    }
}

#Preview {
    ScannerView()
}
