import SwiftUI

struct HomeView: View {
    var body: some View {
        VStack {
            Text("Home")
                .font(.largeTitle)
                .padding()
        }
        .background(Color.white)
    }
}

#Preview {
    HomeView()
}
